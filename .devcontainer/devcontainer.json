{
  "name": "R Assistant Tool",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "apt-get update && apt-get install -y r-base && pip install -r requirements.txt && Rscript -e \"options(repos = c(CRAN = 'https://cran.rstudio.com/')); install.packages(c('rmarkdown', 'knitr'))\"",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "FastAPI Server"
    }
  }
}
