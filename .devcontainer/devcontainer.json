{
  "name": "R Assistant Tool",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "apt-get update && apt-get install -y r-base pandoc && pip install -r requirements.txt",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "FastAPI Server"
    }
  }
}
